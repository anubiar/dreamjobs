export type LoginModel = {
    email : string,
    password : string,
}

export type RegisterModel = {
    email : string,
    username : string,
    password : string,
    name : string,
    phone : string,
}
